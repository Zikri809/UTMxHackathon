import { RoutePlaceholder } from "@/components/route-placeholder"

export default function CropDetailPage() {
  return (
    <RoutePlaceholder
      title="Crop Detail"
      description="This detail view will explain whether a crop is on track and what changed the expected ready date."
    />
  )
}
